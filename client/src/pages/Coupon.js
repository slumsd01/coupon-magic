import React from 'react';
import Auth from '../utils/auth';


const Coupon = () => {
  

  const loggedIn = Auth.loggedIn();

  return (
    <main>
      <div>
        
      </div>
    </main>
  );
};

export default Coupon;
